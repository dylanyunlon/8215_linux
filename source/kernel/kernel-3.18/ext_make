# File existed?
define directory_exist
$(shell if [ -d "$1" ]; then \
           echo $1 ; \
        fi)
endef


SUBDIR := $(call directory_exist,aud_protect/ac83xx/protect) \
          $(call directory_exist,nosuchdir)

all:
	@for d in $(SUBDIR); do make -C $$d ; done 
